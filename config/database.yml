default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  database: rails_runtimes_host_development

test:
  <<: *default
  database: rails_runtimes_host_test

production:
  <<: *default
